import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.evoxed.iooan.app',
  appName: 'feodal-mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
